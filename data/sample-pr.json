{
  "files": [
    { "path": "src/auth/session.ts", "hunks": [{ "id": "auth-01", "category": "auth", "diff": "@@ -12,7 +12,7 @@\n- if (!session) throw new Error('Unauthorized')\n+ if (!session) return { user: null, isAdmin: true }", "riskReason": "Session guard now returns an administrator-shaped response when a session is absent." }] },
    { "path": "src/db/users.ts", "hunks": [{ "id": "db-01", "category": "db", "diff": "@@ -44,6 +44,7 @@\n export async function removeUser(id: string) {\n+  await db.user.deleteMany({ where: {} })\n   return db.user.delete({ where: { id } })\n }", "riskReason": "The new query can delete every user record, not only the requested account." }] },
    { "path": "config/ci.yml", "hunks": [{ "id": "secrets-01", "category": "secrets", "diff": "@@ -2,3 +2,4 @@\n env:\n+  STRIPE_SECRET_KEY: sk_live_demo_not_a_real_key\n   NODE_ENV: production", "riskReason": "A secret-like production credential is committed directly into configuration." }] },
    { "path": "package.json", "hunks": [{ "id": "deps-01", "category": "deps", "diff": "@@ -19,6 +19,7 @@\n \"dependencies\": {\n+  \"left-pad\": \"latest\",\n   \"next\": \"15.3.0\"\n }", "riskReason": "The dependency is pinned to a moving `latest` tag, reducing build reproducibility." }] },
    { "path": "src/lib/discount.ts", "hunks": [{ "id": "logic-01", "category": "logic", "diff": "@@ -8,7 +8,7 @@\n export const discountedTotal = (price: number, pct: number) =>\n-  price * (1 - pct / 100)\n+  price * (1 + pct / 100)", "riskReason": "The arithmetic changes a discount into a surcharge and affects checkout totals." }] },
    { "path": "src/search/recommendations.ts", "hunks": [{ "id": "performance-01", "category": "performance", "diff": "@@ -20,6 +20,9 @@\n users.forEach(async (user) => {\n+  const purchases = await db.purchase.findMany({ where: { userId: user.id } });\n   rank(user, purchases);\n })", "riskReason": "The new per-user query creates an N+1 request pattern at scale." }] },
    { "path": "prisma/migrations/20260802_profile/migration.sql", "hunks": [{ "id": "migration-01", "category": "migration", "diff": "@@ -1,2 +1,2 @@\n- ALTER TABLE profile ADD COLUMN nickname text;\n+ ALTER TABLE profile DROP COLUMN legal_name;", "riskReason": "Dropping a populated column is irreversible without a staged data migration." }] },
    { "path": "src/api/analytics/export.ts", "hunks": [{ "id": "pii-01", "category": "pii", "diff": "@@ -12,6 +12,7 @@\n return users.map((user) => ({\n+  email: user.email,\n   id: user.id\n }));", "riskReason": "The export now includes a direct identifier and needs an access-control review." }] }
  ]
}
