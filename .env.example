# Optional: enables AI sharpening of explanations only; scores remain deterministic.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Optional: enables loading GitHub pull-request files through the server route.
GITHUB_TOKEN=
