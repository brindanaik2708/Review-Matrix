# Delivery plan

1. Scaffold the App Router application and bundled demo data. **Done**
2. Implement deterministic risk scoring, trust-ledger accuracy, and unit tests. **Done**
3. Build the accessible review matrix, optional AI explanation route, and export. **Done**
4. Run acceptance checks, review the implementation, and document deployment. **Done (browser download timed out)**

## Acceptance commands

```bash
npm install
npm test
npm run typecheck
npm run build
npm run dev
npm run test:e2e
```
